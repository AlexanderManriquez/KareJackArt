<!doctype html>
<html lang="es">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>{{#if title}}{{title}}{{else}}Kare Jack Art{{/if}}</title>
    <link rel="stylesheet" href="/css/styles.css">
    <style>
      /* layout fallback for very small environments */
      main { padding: 20px; max-width: 1100px; margin: 0 auto }
    </style>
  </head>
  <body>
    {{> navbar}}

    <main>
      {{{body}}}
    </main>

    {{> footer pageClass=pageClass}}

    <!-- Year is rendered server-side via res.locals.year -->
  </body>
</html>
